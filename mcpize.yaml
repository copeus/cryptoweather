# MCPize manifest - https://mcpize.com
# Generated by mcpize analyze
version: 1
runtime: python
build:
  install: pip install -r requirements.txt
startCommand:
  type: stdio
  command: python3 main.py
  args:
    - main.py
credentials:
  - name: CRYPTOWEATHER_API_URL
    required: false
    description: CryptoWeather API endpoint URL
    mapping:
      env: CRYPTOWEATHER_API_URL
credentials_mode: per_user
